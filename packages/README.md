# `packages`

Reserved for future shared packages (API types, generated OpenAPI clients, validation schemas, or shared configuration).

Do not extract code here until there is a concrete cross-application reuse need. Documentation, contracts, and Cursor rules stay at the repository root.
