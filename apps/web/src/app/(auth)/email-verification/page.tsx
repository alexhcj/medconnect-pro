import {MilestoneStubPage} from '@/components/auth/milestone-stub-page';

export default function EmailVerificationPage() {
	return (
		<MilestoneStubPage
			title="Email verification"
			description="Account recovery and email verification are not wired in the mock identity demo."
		/>
	);
}
