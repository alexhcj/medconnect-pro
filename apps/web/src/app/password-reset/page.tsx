import {MilestoneStubPage} from '@/components/auth/milestone-stub-page';

export default function PasswordResetPage() {
	return (
		<MilestoneStubPage
			title="Password reset"
			description="Password reset is not part of the M1 mock identity milestone."
		/>
	);
}
